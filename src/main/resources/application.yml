spring:
  datasource:
    url: jdbc:mysql://localhost:3306/OneQJob?characterEncoding=utf8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
          show_sql: true
          format_sql: true

  redis:
    host: 127.0.0.1 # 수정: localhost에서 127.0.0.1로 변경 (동일한 의미입니다만, 일관성을 위해 수정)
    port: 6379
#    password: # 비밀번호가 필요한 경우 여기에 추가합니다. (예: password: 'your_redis_password')

  session:
    store-type: redis
    redis:
      namespace: zum:session # 세션 데이터를 저장할 Redis 네임스페이스를 지정합니다.

logging:
  level:
    org:
      hibernate:
        SQL: debug
      springframework:
        security: DEBUG
        web: DEBUG

spring.config.import: classpath:application-secret.yml

#server:
#  servlet:
#    session:
#      cookie:
#        path: / # 적용될 URL 경로
#        name: JSESSIONID # 쿠키의 이름
#        domain: zum.com # 유효 도메인
#        # secure: true # HTTPS 연결에서만 쿠키 전송 (주석 처리 해제하여 사용)
#
#      cookie:
#        path: / # 적용될 URL 경로를 나타냅니다. 예를 들어, path를 "/"로 설정하면 해당 도메인의 모든 경로에서 쿠키가 사용될 수 있습니다.
#        name: JSESSIONID # 이름을 지정합니다.
#        domain: zum.com # 유효 도메인을 지정합니다. 예를 들어, domain을 "zum.com"으로 설정하면 해당 도메인과 그 서브도메인에서 쿠키가 유효합니다.
##        secure: true # 쿠키가 HTTPS(SSL/TLS) 연결을 통해서만 전송되어야 함을 나타냅니다. 즉, HTTPS로 암호화된 연결에서만 쿠키가 전송되어야 합니다.
#
#
#spring:
#  redis:
#    host: 127.0.0.1
#    port: 6379
#    password:
#  session:
#    store-type: redis # 세션 저장소를 지정합니다.
#    redis:
#      namespace: zum:session
